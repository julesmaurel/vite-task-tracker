version: '3.8'

services:

  client:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    links:
      - server

  server:
    build: 
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "3003:3003"

