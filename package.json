{
  "name": "vite-task-tracker",
  "version": "1.0.0",
  "description": "task tracker with MERN stack",
  "scripts": {
    "start": "cd server && npm i && node server.mjs",
    "build": "cd frontend && npm i && npm run build",
    "dev": "concurrently \"cd server && npm i && npm run start\" \"cd frontend && npm i && npm run start\""
  },
  "author": "Jules Maurel",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.1"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
