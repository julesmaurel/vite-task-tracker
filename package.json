{
  "name": "vite-task-tracker",
  "version": "1.0.0",
  "description": "task tracker with MERN stack",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd server && npm i && npm run start\" \"cd frontend && npm i && npm run start\" ",
    "build": "cd frontend && npm i && npm run build && cd ../server && npm i && node server.mjs"
  },
  "author": "Jules Maurel",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.1"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
